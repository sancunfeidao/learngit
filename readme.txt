git is a distributed version control system
git is free software distributed under the GPL

how to deal with git@github.com: Permission denied(publickey).

1.MINGW64 enter ssh-keygen -o to generate public rsa key pair
2.find id_rsa.pub
3.put id_ras.pub's content to github ssh and GPG keys

# learngit
